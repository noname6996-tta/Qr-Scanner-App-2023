<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QrScanner2023Application" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <!-- Màu Status Bar (API 21+) -->
        <item name="android:statusBarColor">#717171</item>
        <!-- Màu chữ/icons trắng hoặc đen (API 23+) -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="BackgroundColor">
        <item name="android:background">@color/main_color_dark</item>
    </style>

    <style name="IconApp">
        <item name="android:background">@drawable/icon_dark_theme</item>
        <item name="android:width">200dp</item>
        <item name="android:height">200dp</item>
    </style>
</resources>